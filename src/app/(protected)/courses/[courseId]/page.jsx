/**TODO:mouloudj
 * techs: tailwind css only
 * no react hooks
 * responsive as well :)
 * use next link to redirect user to the course page under /courses/[courseId]/learn ( cuz (protected) is ignored by nextjs)
 */
import React from "react";

const page = () => {
  return <div>page</div>;
};

export default page;
